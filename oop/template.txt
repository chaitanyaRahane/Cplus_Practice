#include<iostream>

using namespace std;

template <typename T>
class Clculator {

public:
	T add(T x, T y) {
		return x + y;
	}
};
int main(void) {

	Clculator<int> c;
	Clculator<float> d;
	cout << c.add(2, 2) << endl;
	cout << d.add(2.2, 2.2) << endl;

	return 0;
}